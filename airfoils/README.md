# Airfoils

Airfoil coordinate databases and generated STL geometries live here.

- `database/`: source `.dat` coordinate files.
- `generated/`: workflow-generated STL files, including the canonical
  `geometry.stl` used by OpenFOAM dictionaries.
